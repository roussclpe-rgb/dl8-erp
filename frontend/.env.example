# URL base de la API. En desarrollo se deja "/api" y Vite la redirige al
# backend (localhost:3001) vía proxy configurado en vite.config.js.
# En producción, apunta directo a la URL pública del backend, ej:
# VITE_API_URL=https://api.mipanaderia.com/api
VITE_API_URL=/api
